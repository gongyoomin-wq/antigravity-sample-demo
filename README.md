# Antigravity Sample Demo

This is a sample project created via Gemini CLI to demonstrate a Gradio app.

## How to run
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `python app.py`
